@model JabatanViewModel

<form action="/master/jabatan/save" method="post">
    <input type="hidden" asp-for="ExistingID" />
    <input type="hidden" asp-for="IsNew" />    <div class="modal-header bg-primary-700 bg-success-gradient">
        @if (Model.IsNew)
        {
            <h4 class="modal-title">Tambah Data Jabatan</h4>
        }
        else
        {
            <h4 class="modal-title">Edit Data Jabatan</h4>
        }

        <button type="button" class="close text-white" data-dismiss="modal">&times;</button>
    </div>
    <div class="modal-body">
        <div class="form-group">
            <label>Nama Jabatan</label>
            <input type="text" asp-for="Jabatan.NamaJabatan" class="form-control" placeholder="Isi nama jabatan..." autocomplete="off" autofocus />
            <span asp-validation-for="Jabatan.NamaJabatan" class="text-danger"></span>
        </div> 
        <div class="form-group">
            <label>Nama Bidang</label>
            <select asp-for="Jabatan.BidangID" class="form-control cSelect" id="sBidang">
                @if (Model.Jabatan.BidangID != Guid.Empty)
                {
                    <option value="@Model.Jabatan.BidangID" selected="selected">@Model.NamaBidang</option>
                }
            </select>
            <span asp-validation-for="Jabatan.BidangID" class="text-danger"></span>
        </div>
    </div>
    <div class="modal-footer">
        <button type="button" class="btn btn-danger" data-dismiss="modal">Tutup</button>
        <button type="submit" class="btn btn-success">Simpan</button>
    </div>
</form>